<!DOCTYPE html>
<html>
  <head>
    <title>JOURNAL</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

<section class="w-full px-12 bg-gray-800">
    <div class="container flex flex-col flex-wrap items-center justify-around py-5 mx-auto md:flex-row max-w-7xl">
        <div class="relative flex flex-col md:flex-row">
            <a href="#_" class="flex items-center mb-5 font-medium text-gray-900 lg:w-auto lg:items-center lg:justify-center md:mb-0">
                <%= link_to 'Organizer', root_path, class:"mx-auto text-2xl font-black leading-none text-yellow-500 select-none" %><span class="text-indigo-600 text-xl font-black">.</span>
            </a>
            <% if user_signed_in?%>
            <nav class="flex flex-row items-center mb-5 text-base md:mb-0 md:pl-8 md:ml-8 md:border-l md:border-gray-100">
                <a href="#_" ><%= link_to 'Dashoard', categories_path(@category), class: "mr-5 font-medium leading-6 text-white hover:text-yellow-500" %></a>
                <a href="#_" ><%= link_to 'New Category', new_category_path(@category), class: "mr-5 font-medium leading-6 text-white hover:text-yellow-500" %></a>
            </nav>
            <% else %>
            <nav class="flex flex-row items-center mb-5 text-base md:mb-0 md:pl-8 md:ml-8 md:border-l md:border-gray-100">
            </nav>
            <% end %>
        </div>

        <div class="inline-flex items-center ml-5 space-x-6 lg:justify-end">
            <% if user_signed_in?%>
                <%= link_to 'Profile', edit_user_registration_path, class: "inline-flex items-center justify-center px-4 py-2 text-base font-medium leading-6 text-white whitespace-no-wrap bg-yellow-500 border border-transparent rounded-md shadow-sm hover:bg-transparent focus:outline-none focus:ring-2 focus:ring-offset-2" %>  
                <%= link_to 'Sign Out', destroy_user_session_path, method: :delete, class: "inline-flex items-center justify-center px-4 py-2 text-base font-medium leading-6 text-white whitespace-no-wrap bg-yellow-500 border border-transparent rounded-md shadow-sm hover:bg-transparent focus:outline-none focus:ring-2 focus:ring-offset-2" %>
            <% else %>
                <%= link_to 'Sign In', new_user_session_path, class: "inline-flex items-center justify-center px-4 py-2 text-base font-medium leading-6 text-white whitespace-no-wrap bg-yellow-500 border border-transparent rounded-md shadow-sm hover:bg-transparent focus:outline-none focus:ring-2 focus:ring-offset-2" %>
                <%= link_to 'Sign Up', new_user_registration_path, class: "inline-flex items-center justify-center px-4 py-2 text-base font-medium leading-6 text-white whitespace-no-wrap bg-yellow-500 border border-transparent rounded-md shadow-sm hover:bg-transparent focus:outline-none focus:ring-2 focus:ring-offset-2" %>
            <% end %>
        </div>
    </div>
</section>


<% flash.each do |type, msg| %>
    <% if type == "notice" %>
        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative text-center" role="alert" id="flash">
            <strong class="font-bold"><%= msg %></strong>            
        </div>
    <% else %>
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative text-center" role="alert" id="flash">
            <strong class="font-bold"><%= msg %></strong>           
        </div>
    <% end %> 
<% end %>

   
    <%= yield %>

  </body>
</html>


